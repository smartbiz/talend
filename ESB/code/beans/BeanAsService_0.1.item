package beans;

/**
 * This sample demonstrate how can you declare
 * your interface in Java and later use
 * it as your Service interface.
 * 
 */
public interface BeanAsService {

	/**
	 * Simple method that will take two arguments and return one string
	 * 
	 * @param target the target route where to process, can be one of 'one', 'two', 'three'
	 * @param value the value to process
	 * @return
	 */
	public String process(String target, String value);
	
}
